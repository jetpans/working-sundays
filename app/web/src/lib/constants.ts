// API server URL - defaults to production URL, can be overridden in the header
const DEFAULT_SERVER = "sundays.jetpans.com";
const DEFAULT_USERNAME = "operator";

export { DEFAULT_SERVER, DEFAULT_USERNAME };
